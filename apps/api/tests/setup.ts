// Test setup — runs before each test file
// Add global test helpers, mocks, and database setup here

import { afterAll, beforeAll } from 'vitest';

beforeAll(async () => {
  // Future: set up test database connection
});

afterAll(async () => {
  // Future: clean up test database
});
